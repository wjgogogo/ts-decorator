{
  "name": "ts-decorator",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/koa": "^2.13.4",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@koa/router": "^10.1.1",
    "koa": "^2.13.1",
    "reflect-metadata": "^0.1.13"
  },
  "scripts": {
    "start:validator": "npm run build && node './dist/param-validator/validate'",
    "start:router": "npm run build && node './dist/router-injection/index'",
    "start:mobx": "npm run build && node './dist/toy-mobx/index'",
    "build": "tsc"
  }
}
